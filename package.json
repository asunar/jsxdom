{
  "name": "jsxdom",
  "version": "1.0.0",
  "description": "Sample project to use JSX with babel instead of templating engine",
  "main": "index.html",
  "scripts": {
		"build": "babel src -d lib",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Alper Sunar",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.4.0"
  }
}
